# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.output/
.vite/

# Env / secrets
# .env is committed (Vite reads VITE_* into import.meta.env; safe to share).
# .env.local and .env.<mode>.local are per-developer overrides — never commit.
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage/

# Agency / APM runtime
/.do-results.json
/apm_modules/

# Playwright MCP verification outputs (regenerated per session)
/.playwright-mcp/


# APM dependencies
apm_modules/
